import Booking from '../src/Booking'
import chai from 'chai';
import spies from 'chai-spies'
import domUpdates from '../src/domUpdates'
import { bookings } from '../data'
const expect = chai.expect;


describe('See if the tests are running', function() {
    it('should return true', function() {
      expect(true).to.equal(true);
    });

    it('should be able to see the most popular booking date', () => {
        // expect(booking.findMostPopularBookingDate).to.be.be(date)
    });

    it('should be able to see the most available booking date', () => {
        // expect(booking.findMostPopularBookingDate).to.be.be(date)
    });
    
    it('should be search for what rooms are available for a specific date', () => {
        // expect(booking.findMostPopularBookingDate).to.be.be(date)
    });
    
    it('should return all bookings for a valid customer, past and current', () => {
        // expect(booking.findAllBookings).to.be.be(bookingData)
    });

    it('should return a notification if there is no valid data for a customer ', () => {
        //chai spies
        // expect(booking.noCustomerNotification).to.be.be(shown)
    });

    it('should show all available rooms for a specified customer in the Rooms tab', () => {
        //- type
        // - number of beds
        // - bed sizes
        // - bidet availability
        // has this information 
        //available prior for booking
    });

    it('should allow for a booking a new customer', () => {

    });

    it('should allow for a customer to order room service', () => {

    });












    //show a hotel manager
    it('should show all daily room service charges on the main page', () => {

    });

    it('should show all bookings information on the main page', () => {

    });

    it('should allow the admin to search for a specific customer', () => {

    });

    it('should allow the admin to add a new customer', () => {

    });

    it('should make all tabs specific to the last customer that was searched or added', () => {

    });

    it('should notify the admin if there is no valid customer information returned', () => {

    });

    it('should allow the admin to book or unbook a room', () => {

    });

    it('should allow the admin to upgrade a room for a customer', () => {

    });

    it('should allow the admin to calculate the total bill for a customer', () => {

    });

    it('should allow the admin to calculate the total bill for a customer', () => {

    });

    it('should have the date displayed in the main tab', () => {

    });

    it('should have the total revenue for a selected date on the main tab', () => {

    });
    
    it('should have the total rooms available for a selected date on the main tab', () => {

    });

    it('should have the four tabs that bring to specific tabs', () => {

    });





    
    it('should allow the admin to search for a customer on the customer tab', () => {
            //customer
    });

    it('should allow the admin to add a customer on the customer tab', () => {
            //customer
    });

    it('should allow the customer selected to be displayed at the top of the page', () => {
            //customer
    });

    it('should allow the customer selected to be highlighted in separate tabs', () => {
            //customer
    });

    it('should notify the admin if there is no valid data for specified, searched customer', () => {
            //customer
    });

    it('should notify the admin if there is no valid data for specified, searched customer', () => {
        //customer
    });








    it('should display all orders for the specified date on the orders tab', () => {
        //order
    });
    
    it('should allow me to search for all orders of a specified date', () => {

    });
    
    it('should allow me to search for all orders of a specified date', () => {

    });

    it('should display the orders of a specified customer, if a customer has been specified', () => {

    });

    it('should show the breakdown of dates and dollars of room service for a specified customer', () => {

    });

    it('should show the total dollar amount of room service for a specified customer', () => {

    });

    it('should show the total dollar amount of room service by a date for a specified customer', () => {

    });

    it('should show the total dollar amount of room service by all dates for a specified customer', () => {

    });

    it('should show the total dollar amount of room service by all dates for a specified customer', () => {

    });


  });
